// Will contain the redux toolkit logic to manage all the state of the data used in this application
import { configureStore } from "@reduxjs/toolkit";

const store = configureStore({
  reducer: {},
});

export default store;
